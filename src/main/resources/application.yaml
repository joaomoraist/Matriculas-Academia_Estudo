spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gym_frequency
    username: postgres
    password: 0710

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect